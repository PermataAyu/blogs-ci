{
  "name": "exercise4a",
  "version": "1.0.0",
  "description": "exericse",
  "license": "MIT",
  "author": "Novelty",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development node --watch index.js",
    "test": "cross-env NODE_ENV=test node --test",
    "build:dev": "rmdir /s /q dist && cd frontend && npm run build && robocopy dist ../dist /E",
    "start:test": "cross-env NODE_ENV=test node --watch index.js"
  },
  "dependencies": {
    "@playwright/test": "^1.60.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "lodash": "^4.17.21",
    "mongodb": "^7.0.0",
    "mongoose": "^9.1.5"
  },
  "devDependencies": {
    "supertest": "^7.1.4"
  }
}
